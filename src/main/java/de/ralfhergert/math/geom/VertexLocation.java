package de.ralfhergert.math.geom;

/**
 * This enum covers all three possible results for
 * {@link Mesh#calcVertexLocation(Vertex)}.
 */
public enum VertexLocation {
	InBounds,
	OnBounds,
	OutBounds
}
